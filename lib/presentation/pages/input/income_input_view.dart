import 'package:flutter/material.dart';

class IncomeInputView extends StatelessWidget {
  const IncomeInputView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('収入入力ページ'));
  }
}
