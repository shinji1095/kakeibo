# 35日家計簿（Flutter） 要件資料セット

## 初期リリース（v1.x.x）スコープ
- 取引の追加/編集/削除、期間（35/42）に基づく一覧・内訳・推移、CSVエクスポート、カテゴリ管理、年間スケジュール、設定（テーマ/言語等）
- 初期カテゴリ（デフォルトセット）は `06_カテゴリ一覧.csv` を正とする（ユーザーは追加/編集/削除可能）
  - 収入カテゴリと支出カテゴリは完全に分離
  - カテゴリ削除時は「未分類」に置換

## 初期リリース対象外（次期）
- F036（リマインダー機能）および P08（リマインダー管理画面）
  - 本資料では機能ID/画面は保持するが、テスト要求・非機能要件の優先度を `Could` に変更し、初期リリースの受入対象から除外する。

## バージョニング方針
- リリースは `v1.x.x`
- 機能追加は `vx.1.1`
- バグ修正は `vx.x.1`

## 非機能要件（TBD）
- 未決定の非機能要件はTBDとし、今回のバージョンでは評価対象外とする。

## ファイル一覧
- 01_画面リスト.csv
- 02_機能要件定義.csv
- 03_画面遷移.md
- 04_非機能要件.csv
- 05_テスト要求管理表.csv
- 06_カテゴリ一覧.csv
